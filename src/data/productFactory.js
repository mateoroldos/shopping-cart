const productFactory = (id, name, surname, birth, country, imageName, position, price) => {
  return { id, name, surname, birth, country, imageName, position, price };
};

export default productFactory;
